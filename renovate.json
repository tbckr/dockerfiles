{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base",
    ":semanticCommits",
    ":preserveSemverRanges",
    ":enablePreCommit"
  ],
  "stabilityDays": 7,
  "ignorePaths": [],
  "labels": [
    "dependencies"
  ],
  "vulnerabilityAlerts": {
    "labels": [
      "type:security"
    ],
    "stabilityDays": 0
  },
  "packageRules": [
    {
      "description": "Automatically merge minor and patch-level updates",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest"
      ],
      "automerge": true,
      "automergeType": "branch"
    }
  ],
  "regexManagers": [
    {
      "description": "Update installed software in docker images. Versions are set in ENV variables.",
      "fileMatch": [
        "^Dockerfile$"
      ],
      "matchStrings": [
        "^# renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?\\sENV .*?_VERSION=(?<currentValue>.*)$"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    },
    {
      "description": "Update docker base images. Versions are set in ARG variables instead of in the FROM command.",
      "fileMatch": [
        "^Dockerfile$"
      ],
      "matchStrings": [
        "ARG IMAGE=(?<depName>.*?):(?<currentValue>.*?)@(?<currentDigest>sha256:[a-f0-9]+)$"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "description": "Update docker base images in multi-stage builds. Versions are set in ARG variables instead of in the FROM command.",
      "fileMatch": [
        "^Dockerfile$"
      ],
      "matchStrings": [
        "ARG MULTISTAGE_IMAGE_.*?=(?<depName>.*?):(?<currentValue>.*?)@(?<currentDigest>sha256:[a-f0-9]+)$"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
